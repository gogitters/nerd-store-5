<h1>Your Cart:</h1>

<% @carted_products.each do |carted_product| %>
  <h3><%= carted_product.product.name %> - <%= carted_product.quantity %></h3>
  <%= link_to "Remove from Cart", "/carted_products/#{carted_product.id}", method: :delete %>
<% end %>

<div>
  <%= button_to "Checkout", "/orders" %>
</div>
